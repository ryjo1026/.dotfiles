# No more Python 2
# alias python='python3'
{{- if eq .chezmoi.arch "arm64" }}
export PATH=/opt/homebrew/bin:~/.local/bin:$PATH
{{- else }}
export PATH=/usr/local/bin:~/.local/bin:$PATH
{{- end }}

# nvm is lazy-loaded via the lukechilds/zsh-nvm antidote plugin (see dot_zsh/nvm.zsh.tmpl).
